#pragma once

// Forward declarations of protobuf types for use in class declarations
// Actual instantiation of generated proto files should only occur in .cpp files

namespace downlink_proto {
    class Timestamp;
    class PowerSupplyMetrics;
    class IMUMetrics;
    class SpaceComputerMetrics;
    class DigitalMetrics;
    class AnalogMetrics;
    class SystemMetrics;
}
